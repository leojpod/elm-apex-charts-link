class ApexChart extends HTMLElement {
  constructor() {
    super();
  }
  // put the code that reads the chart-data provided by the elm side
  // -> transform it into a nice chart (possibly with some default)
}

customElements.define("apex-chart", ApexChart);
